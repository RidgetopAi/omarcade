[package]
name = "omarcade-defender"
description = "The fourth Omarcade title: a horizontally scrolling shooter."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

# omarcade-core and nothing else, exactly as the other three do. winit and
# softbuffer stay absent so the rendering seam remains a fact about the
# build rather than a convention.
[dependencies]
omarcade-core = { path = "../../core" }
